# Continuity handoff のレビュー

## 目的
final draft を唯一の根拠に continuity handoff を検証する。

## 応答方針
事実誤認、推測、重要な状態変化の脱落、次 writer を誤誘導する記述だけを issue にする。以下の ### 指摘対象 方針に従う。レビュー対象に根拠があり、改訂工程で具体的に修正できる問題に限定する。severity は後続工程への影響度で決める。軽微に見える表記・用語でも、後続工程で固定化・拡散する場合は指摘する。修正可能な差分に限定し、出版可否、総評、長所、スコアは書かない。`issues` が空配列なら改訂不要、1件以上なら改訂を継続する。問題がない場合は、無理に指摘を作らず `issues` を空配列にする。入力キーワードまたは前工程JSONに明示された期間、職業、役割、性別、タイトル、ジャンル、固有名は正として扱い、後続工程で具体化できる未定義要素だけを指摘する。自然なカタカナ語、英語表記、英字略語、一般的なジャンル語、固有名詞、日本語として成立する漢語は言語純度の問題にしない。`issues` は最大8件に限定する。各 issue は必ず `severity`、`field`、`description`、`suggestion` を含める。`before`/`after` は任意の補足（修正前後の引用・例示）であり、省略してもよい。`description` と `suggestion` は短文にする。二重引用符を書かない。日本語文脈で不自然な簡体字、中国語構文、英語混在、ハングル混在を指摘する。指摘時は問題文字列を引用する。自然なカタカナ語、英語表記、英字略語、一般的なジャンル語、固有名詞、日本語として成立する漢語は問題にしない。

## 実行指示
問題がない場合は `issues` を空配列にする。

## レビュー対象
### final draft
{draft}

### candidate summary
{summary}

## 出力仕様
下記のスキーマに適合する JSON のみ出力すること。

{schema}
