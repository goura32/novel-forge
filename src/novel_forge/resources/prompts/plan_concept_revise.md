# シリーズ企画改訂

## 目的
レビュー指摘を解消した完全なシリーズ企画を返す。

## 応答方針
`issues[].field` に関係しないフィールドは原則として元の値を保持する。整合性調整が必要な場合だけ、最小限変更する。明示的な指摘がない限り変更しない。

## 実行指示
### 改訂対象
{current_plan}

### レビュー指摘
{review}

main_characters と locations を空にせず、slugを推測・正規化に頼らず有効値にする。

## 出力仕様
下記のスキーマに適合する JSON のみ出力すること。

{schema}
