{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DraftAudit",
  "type": "object",
  "properties": {
    "issues": {
      "description": "Draft の不備リスト。空リストは承認を意味する。各要素は severity と detail を持つ。",
      "type": "array",
      "items": {
        "type": "object",
        "description": "単一の指摘。severity で重大度を、detail で違反箇所と根拠を具体的に示す。",
        "required": [
          "severity",
          "detail"
        ],
        "properties": {
          "severity": {
            "type": "string",
            "description": "指摘の重大度。blocker は出版不能、major は読者体験を損なう、minor は軽微な不備を示す。",
            "enum": [
              "blocker",
              "major",
              "minor"
            ]
          },
          "detail": {
            "type": "string",
            "description": "違反箇所と根拠を具体的に記述する。WriterView のどの制約に反しているかを明示する。"
          }
        }
      }
    }
  }
}
