{
  "type": "object",
  "required": [
    "contract_id",
    "canon_seed",
    "final_resolution",
    "volume_purposes"
  ],
  "properties": {
    "contract_id": {
      "type": "string",
      "description": "最終 series slug。`^[a-z0-9_]{1,40}$` に一致し、request の existing_slugs と重複しない stable ID。",
      "pattern": "^[a-z0-9_]{1,40}$"
    },
    "canon_seed": {
      "type": "object",
      "description": "空でないシリーズ開始時点の Canon。series ID、title、logline、初期 entity/state を含む JSON object。",
      "minProperties": 1
    },
    "final_resolution": {
      "type": "string",
      "description": "最終巻で必ず具体的に解決する、request に含まれる中心の呪い・鍵となる artifact・宮廷対立・二人の関係の到達状態を一文で結んだ最終解決契約。"
    },
    "volume_purposes": {
      "type": "array",
      "minItems": 1,
      "description": "各巻の one-line purpose。ordinal は 1 から昇順・重複なし。章や scene beats は書かない。",
      "items": {
        "type": "object",
        "description": "一巻分の purpose record。ordinal と purpose を必ず含める。",
        "required": [
          "ordinal",
          "purpose"
        ],
        "properties": {
          "ordinal": {
            "type": "integer",
            "minimum": 1,
            "description": "シリーズ内での巻番号。1 からの昇順で重複させない。"
          },
          "purpose": {
            "type": "string",
            "description": "その巻がシリーズ全体で果たす一文の役割。章、scene、具体的 beat、Canon patch は書かない。"
          }
        }
      }
    }
  }
}
