{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SceneRenderOutput",
  "type": "object",
  "required": ["content", "coverage"],
  "properties": {
    "content": {
      "description": "シーン本文。WriterView の制約に厳守し、約500〜5000文字の範囲で書くこと。",
      "type": "string"
    },
    "coverage": {
      "type": "object",
      "description": "本文には含めない検証用の obligation evidence。各 required beat と end constraint を本文内で完了したことを、本文からの完全一致引用で示す。",
      "required": ["evidence"],
      "properties": {
        "evidence": {
          "type": "array",
          "description": "要件ごとの完了証拠。WriterView に必須義務がない場合だけ空配列にする。",
          "items": {
            "type": "object",
            "description": "一つの required beat または end constraint を本文で完了した証拠。",
            "required": ["obligation", "draft_quote"],
            "properties": {
              "obligation": {"type": "string", "enum": ["required_beat", "end_constraint"], "description": "証明する WriterView 義務の種類。"},
              "beat_index": {"type": "integer", "minimum": 0, "description": "required_beat のゼロ始まり index。end_constraint には出力しない。"},
              "draft_quote": {"type": "string", "description": "この obligation の完了を示す content 内の完全一致引用。"}
            }
          }
        }
      }
    }
  }
}
