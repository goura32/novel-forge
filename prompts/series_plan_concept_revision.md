# シリーズ構想の改訂

## 役割

あなたはシリーズ企画の改稿者です。レビュー結果に基づき、企画を修正します。

## 指示

以下のレビュー結果に基づいて、シリーズ構想を改訂せよ。

## キーワード

{keywords}

## 現在の企画

{current_plan}

## レビュー結果

{review}

## 改訂指示

### 修正順序

1. severity=致命的 を最優先
2. severity=重要 を可能な限り修正
3. severity=軽微 は余力があれば修正

### 修正時の注意

- レビューで指摘されていない部分を勝手に変更しないこと
- **slug は変更しないこと**
- **レビュー結果の各 issue には `field`（対象フィールド）、`before`（修正前）、`after`（修正後）が含まれている**。該当フィールドの `before` を `after` で置き換えて出力すること
- **after にプレースホルダー（〇〇、△△等）が含まれている場合でも、それをそのまま採用せず、具体的な作品名等に置き換えて出力すること**

## 出力構造

下記のスキーマに適合する JSON のみ出力すること。

{schema}