{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SeriesPlanConceptReview",
  "description": "レビュー結果。各 issue は対象フィールド(field)と修正前後(before/after)を明示し、LLMがJSON単体で修正可能にする。",
  "type": "object",
  "required": ["issues"],
  "properties": {
    "issues": {
      "description": "指摘事項のリスト。各要素は修正対象のフィールドを特定し、具体的な差分を示す。",
      "type": "array",
      "items": {
        "type": "object",
        "required": ["field", "category", "description", "suggestion", "before", "after"],
        "properties": {
          "severity": {
            "description": "修正の緊急性。致命的=必須修正、重大=必須修正、重要=強推奨、軽微=任意。",
            "type": ["string", "null"],
            "enum": ["致命的", "重大", "重要", "軽微"]
          },
          "field": {
            "description": "修正対象の企画フィールド名（title, logline, genre, target_audience, themes, selling_points, world_summary, world_rules等）",
            "type": "string"
          },
          "category": {
            "description": "指摘のカテゴリ（あらすじ、ジャンル、世界観、ターゲット、言語）",
            "type": "string"
          },
          "description": {
            "description": "問題の説明。何がどう問題かを具体的に記述。",
            "type": "string"
          },
          "suggestion": {
            "description": "修正の提案。どう改善すべきかの方向性を示す。",
            "type": "string"
          },
          "before": {
            "description": "修正前のテキスト（当該フィールド内の該当箇所を引用）。配列フィールドの場合は該当要素のみ。",
            "type": "string"
          },
          "after": {
            "description": "修正後のテキスト。beforeを置き換える完成形。プレースホルダー禁止、即採用可能な品質。",
            "type": "string"
          }
        }
      }
    }
  }
}
