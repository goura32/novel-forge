{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "VolumeDesign",
  "type": "object",
  "description": "LLM は chapters ごとに title, purpose を生成する。number と chapter_number は engine で機械採番されるため含めない。",
  "required": [
    "chapters"
  ],
  "properties": {
    "title": {
      "description": "巻タイトル。10〜100文字推奨。",
      "type": "string"
    },
    "premise": {
      "description": "巻の前提・あらすじ。200〜800文字推奨。",
      "type": "string"
    },
    "chapters": {
      "description": "章のリスト。2〜8個推奨。",
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "required": [
          "title",
          "purpose"
        ],
        "properties": {
          "title": {
            "description": "章タイトル。5〜100文字推奨。",
            "type": "string"
          },
          "purpose": {
            "description": "章の役割。",
            "type": "string",
            "minLength": 1,
            "enum": [
              "導入",
              "展開",
              "転換",
              "クライマックス",
              "収束"
            ]
          }
        }
      }
    }
  }
}